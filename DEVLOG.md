# 개발 일지

## 2026-09-15 — 3일 차 마무리: 로컬 인식·번역 연결과 품질 확인

복습 초안: [3일 차 TIL](docs/TIL/2026-09-15.md).

### 오늘 완료한 것

- Codex가 로컬 Whisper base 한국어 인식과 수정 가능한 원문 입력란을 연결했다.
- 녹음 종료 후 인식, 녹음 다시 인식, 취소와 삭제 시 이전 결과 무효화를 구현했다.
- Ollama를 통해 수정된 한국어 → 영어 번역 → 해당 영어의 한글 발음이라는 두 단계 처리를 연결했다.
- 사용자는 직접 녹음·원문 수정·재인식·번역을 시험하고 화면과 실제 말한 내용을 공유했다.
- 음성 인식과 번역을 각각 ISpeechRecognizer, ITranslationService로 분리했다. Ollama 모델은 생성자 인자로 교체 가능하다. 별도 선택 UI와 외부 API 구현은 없다.
- qwen3:4b와 현재 기본값 qwen3:8b는 최종 모델이 아닌 MVP 후보다. 발음 생성은 LLM 직접 생성과 프로그램의 형식 검증을 조합하며 별도 발음 변환·교정 모듈은 없다.

### 사용자 테스트 결과와 해석

| 사례 | 사용자 보고·화면 | 판단과 미확인 사항 |
| --- | --- | --- |
| 1. 두 문장의 게임 상황 설명 | 사용자가 발화와 한국어 원문이 같다고 확인. 인식 약 1.0초. 영어·발음 생성 실패 표시 | 한국어 인식은 사용자 확인 범위에서 성공. 코드상 최종 결과 검증 문구로, 발음의 빈 값·길이·허용 문자 조건 실패가 의심된다. 당시 모델 응답이 남지 않아 정확한 원인은 미확정 |
| 2. A 거점에서 막자 | 한국어 `에이거점에서 막자` → `Hold at the Agar point.`. 인식 약 0.9초, 번역 약 0.5초 | ‘에이’를 A라는 기호와 거점으로 분리하지 못한 표기가 번역 해석에 영향을 준 것으로 보인다. ‘어거 포인트’는 Whisper 원문이 아니라 번역·발음 단계의 출력. 정확한 원문으로 수정한 비교 시험은 아직 미실시 |
| 3. 아크 스테이션 이동 질문 | 한국어 원문은 사용자 발화와 같다고 보고. `I need to head to Ark Station now. Do you know where that is?` 생성. 인식 약 1.0초, 번역 약 3.2초 | 비교 번역기의 `go to ... right now`와 핵심 의미는 같다. 고유명사의 공식 영문 표기는 미확인. 한글 발음 `웨어 쑤어 티즈` 등은 부정확 |

원음을 직접 청취한 검증은 아니다. 시간은 제공된 화면의 단계별 표시이며 말하는 시간까지 포함한 전체 지연이나 게임 중 성능이 아니다. 3번의 ‘Do you know’에서 ‘유’가 누락됐다는 앞선 답변은 화면 오독이었다. 화면에는 ‘두 유 노우’가 있으므로 그 누락 판단은 정정한다.

### 오늘의 검증과 한계

- 오디오 형식 변환·버퍼·모델 실행 검사와 번역 요청·응답·취소·재시도·WPF 상태 검사가 통과했다. 자세한 단계별 검증은 아래 작업 기록을 따른다.
- 테스트 통과는 기능 연결과 일부 오류 처리를 확인한 것이다. 번역 의미와 한글 발음 품질이 충분하다는 뜻은 아니다.
- 발음 실패 시 영어도 함께 표시하지 않아 어느 단계가 실패했는지 화면에서 구분하기 어렵다. 오류 안내 개선은 아직 구현하지 않았다.
- 실제 음성·원문·번역을 앱에서 파일로 저장하지 않는다. 로컬 모델과 Ollama 실행 도구는 디스크에 설치돼 있다. 유료 외부 API를 호출하지 않았다.

### 다음 시작점

새 기능으로 넘어가기 전에 기존 결과를 분리해서 시험한다. `A 거점에서 막자.`로 원문을 직접 수정한 번역과 1번 문장을 나눠 번역한 결과를 확인한다. 기호·숫자·방향·부정·고유명사를 포함한 짧은 문장으로 말한 내용, 인식 원문, 수정 여부, 영어, 발음, 시간을 함께 비교한다. 한글 발음 모듈 분리는 후속 평가 대상이며 아직 방식은 미선정이다.

오늘 마무리 작업은 문서 작성이다. 새 기능·모델 변경·추가 음성 테스트는 진행하지 않았다. 아래는 당일 단계별 기록이며 최종 상태와 사용자 확인 결과는 이 마무리 기록을 우선한다.

## 2026-09-15 — 모델 후보 상태와 발음 생성 경계 정리

- qwen3:4b 및 현재 기본 qwen3:8b를 최종 모델이 아닌 MVP 후보로 명시했다.
- 기존 ITranslationService 교체 경계를 유지하고 Ollama 모델 이름을 생성자 인자로 지정할 수 있게 보완했다. 다른 로컬 후보를 두 단계 요청에 적용하는 검사를 추가했다.
- 현재 발음은 ‘LLM의 직접 생성 + 프로그램의 형식 검증’이며 별도 발음 변환·교정 로직은 없음을 기록했다. 향후 발음 모듈 분리 필요성을 평가했으며 실제 분리·새 엔진 구현은 하지 않았다.

## 2026-09-15 — 로컬 영어 번역과 한글 발음의 MVP 연결

- 사용자 요청: 로컬로 먼저 진행하고 추후 API로 교체할 수 있게 하되 구조를 복잡하게 만들지 않는다.
- 구현: ITranslationService/TranslationResult로 경계를 두고 Ollama 구현 하나를 연결했다. ‘번역하기’는 수정된 현재 원문을 사용하며 영어·한글 발음을 표시한다. 원문 수정 시 결과 무효화, 취소, 재시도, 창 닫기와 중복 요청 방지를 포함했다.
- 설치: 사용자 승인 후 Ollama 0.34.0과 qwen3:4b, qwen3:8b 모델을 준비했다. 현재 앱은 8B를 사용하며 비교용 4B도 디스크에 남아 있다. 외부 유료 API를 사용하지 않는다.
- 선택: 작은 모델·추론 모드·지시문을 비교한 뒤 영어 번역 → 해당 영어의 한글 발음이라는 두 단계 요청을 로컬 서비스 안에서 처리한다. API 교체 경계는 한 메서드로 유지했다.
- 검증: 요청·응답 형식, 잘린 결과, 모델 누락, 연결 실패, 시간 초과·취소 및 WPF 원문 수정·늦은 결과 폐기·재시도·창 닫기 검사 통과. 실제 로컬 모델에 시험 문장 7개를 요청했다. 최종 시험 첫 문장 약 2.9초, 이후 약 0.3~0.4초(고정 성능 보장 아님).
- 품질 한계: 영어 번역은 시험한 의미·부정문에서 개선됐지만 한글 발음은 일부 단어 오류가 남았다. 예: come을 ‘콜럼’으로 표기. 형식 검증 통과와 언어 품질 통과를 구분하며 발음 품질 개선은 미완료다. 최근 대화 문맥·실게임 성능은 아직 검증하지 않았다.
- 사용자 확인 보충: 앞선 한국어 인식 화면에서 7.7초 음성의 원문, 약 1초 인식 표시, 직접 수정과 재인식을 확인했다. 이번 번역 화면의 사용자 확인은 다음 단계다.
- 다음: 실제 입력 → 원문 수정 → 번역하기 흐름을 확인하고 발음 정확도 개선 방식과 게임 중 자원 영향을 평가한다. 당일 TIL과 커밋은 아직 마무리하지 않았다.

## 2026-09-15 — 로컬 Whisper 한국어 원문 연결

- 사용자 선택: 로컬 Whisper, 녹음 종료 후 한국어 원문 표시 및 선택적 수정.
- 구현: Whisper.net 1.9.1 CPU 런타임과 다국어 base 연결, 모노 16kHz 변환, 비동기 인식·처리 시간 표시, 취소·재시도, 지우기·새 입력·종료 시 원문 초기화.
- 모델 약 148MB 다운로드와 SHA-256 검증 완료. 음성·원문은 서버로 전송하거나 파일로 저장하지 않는다.
- 검증: 기존 음량·WAV·수집 제한 검사 통과. 형식 변환·진폭·길이·취소 검사 통과. 실제 모델 합성 신호 인식 실행 약 2.0초 확인(한국어 정확도 검사가 아님).
- 문제 해결: 확장 WAV의 WaveFormatExtraData 형식을 정규화하여 변환 실패 해결.
- 추가 검증에서 실행 중 취소 시 네이티브 엔진 종료를 발견했다. 내부 계산 완료 후 결과 폐기 방식으로 수정하고, 취소 후 재인식 및 모델 누락 검사까지 통과했다. 취소 완료에는 현재 계산 시간이 필요할 수 있다.
- 다음: 사용자가 짧은 한국어 발화의 원문·수정·새 입력·취소를 확인하고 실제 처리 시간과 정확도를 평가한다. 번역·발음 생성과 GPU 최적화는 아직 없다.
- 아래 ‘표시 영역 준비’는 엔진 선택 전의 중간 기록이다. 현재 구현 상태는 이 기록을 우선한다.

## 2026-09-15 — 한국어 원문 표시 영역 준비

- 사용자가 녹음 종료 후 원문을 표시하고 필요하면 수정하는 방식을 선택했다.
- 한국어 원문 TextBox와 상태 안내 영역을 추가했다. 엔진 미연결 상태이므로 현재는 읽기 전용이며 실제 결과를 표시하거나 편집하는 단계까지 구현한 것은 아니다.
- 첫 음성 인식 엔진은 아직 미선정이다. 로컬 Whisper, 유료 API, Windows 공식 로컬 API 검토 중 사용자 선택을 기다린다. 모델 다운로드와 외부 음성 전송은 하지 않았다.
- 화면 변경 후 빌드가 경고 0개·오류 0개로 성공했다. 실제 인식·수정·재시도 동작 검증은 엔진 연동 이후 수행한다.

## 2026-09-14 — 2일 차 마무리: 마이크 입력과 메모리 음성 수집

복습 초안: [2일 차 TIL](docs/TIL/2026-09-14.md). 기술 결정과 실제 문제 해결은 각각 [기술 결정 기록](docs/DECISIONS.md), [문제 해결 기록](docs/TROUBLESHOOTING.md)에 정리했다.

### 완료한 개발

- Codex가 마이크 선택·새로고침, 시작·종료 버튼, 입력 음량 게이지를 구현했다.
- 종료 시 최신 입력을 WAV 바이트로 메모리에 보관하고, 길이·용량을 표시하도록 확장했다.
- 수집한 음성 지우기를 추가했다. 새 입력 시작과 창 닫기에도 이전 음성을 제거한다.
- 최대 60초 또는 원시 오디오 16 MiB로 입력을 제한했다. 디스크 저장·외부 전송·음성 재생은 구현하지 않았다.
- 실제 제품 개발을 우선하고 학습용 예시 수정을 별도로 추가하지 않는 협업 지침을 AGENTS.md에 기록했다.

### 사용자 확인과 검증 범위

- 사용자가 실제 마이크로 말할 때 음량 게이지가 움직이는 것을 확인했다.
- 사용자가 수집 완료 표시를 확인하고, ‘음성 지우기 → 마이크 시작 → 마이크 종료 → 음성 지우기’를 직접 수행했다.
- 순서대로 제공한 화면에서 수집 없음, 입력 중, 12.0초·4503.8 KiB 수집 완료, 다시 수집 없음 상태를 확인했다.
- Codex의 음량 계산·WAV 구조·버퍼 제한 검사와 실제 마이크 수집 검사는 앞선 작업 기록과 같다. 화면 확인만으로 녹음 내용의 청취 품질이나 음성 인식 정확도까지 검증된 것은 아니다.
- 60초 자동 종료의 사용자 화면 확인, 장치 분리·권한 거부 상황은 남아 있다.

### 검토한 대안과 결정

- 클라우드 음성 인식 API와 로컬 음성 인식을 비교했다. ChatGPT 구독과 API 요금은 별개이며, 오늘 앱에서 유료 API를 호출하지 않았다.
- Windows Live Captions를 비교 대상으로 검토하고 Windows AI 음성 인식 API, whisper.net을 로컬 후보에 추가했다. 설치·연동·성능 측정은 아직 하지 않았다.
- API는 서버에서 인식 계산을 수행하지만 네트워크·서버 지연과 사용료가 있다. 로컬은 인식 API 사용료가 없지만 게임과 CPU/GPU·메모리를 공유한다. 로컬 부하는 네트워크 핑과 구분해서 평가한다.
- 다음 구현은 ‘녹음 종료 후 인식’ 흐름을 유지하고, 음성 인식 구현을 공통 인터페이스 뒤에 분리하는 방향으로 합의했다. 인터페이스와 엔진 구현은 아직 추가하지 않았다.
- 번역·한글 발음 생성은 음성 인식과 별도 단계다. 로컬 인식만으로 전체 기능이 완성되는 것은 아니다.

### 다음 개발 시작점

1. PC와 Windows 환경, 한국어 지원·배포 조건을 확인하고 첫 음성 인식 엔진을 선택한다.
2. 수집한 음성을 인식해 한국어 원문을 표시하는 작은 기능을 구현한다.
3. 같은 짧은 게임 문장으로 정확도, 종료 후 응답 시간, 게임 실행 중 자원 사용을 비교한다.

게임 음소거 연동·전역 단축키·실시간 인식·번역·오버레이는 오늘 구현 범위에 포함하지 않았다. 아래 기록은 당일 각 단계 시점의 기록이며, 수동 확인 결과와 다음 작업은 이 마무리 기록을 우선한다.

## 2026-09-14 — 입력 구간을 메모리에 수집

- 목표: 마이크 시작부터 종료까지 음성을 보관하여 후속 STT 연동에 사용할 데이터를 준비한다.
- 구현: RecordingBuffer에 프레임 단위 오디오를 복사하고 종료 시 WAV 바이트를 생성한다. UI에 진행 시간, 수집 길이·용량 및 지우기 버튼을 추가했다.
- 제한: 최대 60초 또는 원시 오디오 16 MiB. 한도 도달 시 자동 종료. 최신 입력 하나만 보관하며 새 시작·지우기·창 닫기 때 제거한다. 오류 입력은 폐기한다.
- 검증: 기존 음량 검사 11개, 빈 입력, WAV 읽기·길이, 패킷 원본 변경 후 보존, 시간·용량 제한 검사 통과. 실제 마이크 수집을 두 차례 WAV로 읽고 재시작·입력 중 해제까지 확인했다.
- 문제 해결: WAV 검사에서 1바이트 읽기가 블록 정렬 제약에 걸려 테스트를 한 프레임 읽기로 수정했다.
- 한계: STT 서비스·API 키는 아직 미선정. 실제 인식이나 업로드, 로컬 파일 저장, 재생은 구현하지 않았다. 무음 판별 및 화면의 60초 자동 종료는 사용자 수동 확인이 남아 있다.
- 다음: 화면에서 수집 완료·지우기·재시작을 확인한 뒤 STT 공급자와 비용·인증 방식을 정한다.
- 기존 마이크 음량 게이지는 사용자가 실제 발화로 정상 동작을 확인했다. 이번 코드의 커밋·Push는 하지 않았다.

## 2026-09-14 — 버튼 기반 마이크 입력

### 오늘 목표

마이크를 선택하고 시작·종료 버튼으로 입력을 제어하며 음량을 확인한다.

### 오늘 한 것

- 프로젝트 지침 AGENTS.md 작성(앞선 요청). 해당 파일은 아직 미커밋 상태다.
- NAudio.Wasapi 2.2.1 추가, 마이크 입력 및 음량 계산 파일 분리.
- 입력 장치 선택·새로고침, 시작·종료 버튼, 음량 막대와 상태 표시 추가.
- 입력 중 장치 선택과 중복 시작을 막고, 입력 종료·창 닫기 시 마이크 자원을 해제하도록 구현.
- 기존 고정 영어·한글 발음 예시 유지. README와 기술 결정 기록 갱신.

### 성공한 것

- 앱 빌드 오류·경고 0개.
- PCM/실수 오디오의 무음, 음수, 최대 음량, 불완전 버퍼 등 계산 검사 11개 통과.
- 이 PC에서 활성 마이크 1개 확인. 실제 입력 시작·종료 2회, 재시작, 입력 중 Dispose 검사 통과. 검사 음성은 저장·전송하지 않음.

### 막힌 것

패키지 다운로드가 제한된 네트워크에서 실패하여 승인된 권한으로 NuGet 복원을 완료했다.

### 새로 알게 된 것

공유 모드 입력, 장치별 오디오 형식, 종료 시 자원 해제, 주기적 UI 갱신을 실제 기능에 적용했다. 사용자 학습 여부를 별도로 평가하지 않았다.

### 다음 작업

사용자 화면에서 말소리에 따른 막대 변화, 버튼 조작 및 장치 분리·재연결을 확인한다. 이후 STT 연결을 위한 입력 구간 수집 방식을 검토한다.

### 검증 한계 및 Git 상태

자동 검사는 마이크 시작·종료와 음량 계산까지다. 실제 발화에 따른 화면 변화, 장치 분리, 권한 거부는 수동 확인이 남아 있다. STT·AI 번역·게임 송신 제어는 구현하지 않았다. 오늘 변경의 커밋·Push와 TIL 마무리는 사용자 요청에 따라 당일 작업 종료 후 진행한다.

전일 기록 보충: 9월 13일 일지 작성 이후 첫 커밋과 GitHub Push를 사용자가 완료했다. 오늘 작업 시작 시 master가 로컬 origin/master와 일치하고 작업 폴더가 깨끗한 것을 확인했다.

## 2026-09-13 — 첫 WPF 앱과 화면 수정 실습

### 날짜

2026-09-13 (한국 시간)

### 오늘 목표

기준 명세를 읽고 Windows 기술 스택을 선택한다. 작은 창에서 버튼을 누르면 영어와 한글 발음이 표시되게 만들고, 직접 값을 수정해 결과를 확인한다.

### 오늘 한 것

- `PROJECT_SPEC.md` 전체를 읽고 C#/WPF, Python/PySide6, TypeScript/Electron 후보를 비교했다.
- C# + .NET 10 + WPF로 첫 앱을 만들기로 했다. NAudio/WASAPI는 후속 오디오 단계의 추천 방향이며 아직 설치하지 않았다.
- 기존 PC에는 .NET 런타임만 있고 SDK가 없어 .NET SDK 10.0.401을 설치했다.
- Codex가 WPF 프로젝트, 기본 창, 버튼 클릭 함수, README, .gitignore를 작성했다.
- 사용자가 Cursor에서 영어 글자 크기를 24 → 30으로 수정하고 저장했다.
- 사용자가 예시를 `I'll come with you.` / `아일 컴 위드 유`로 수정하고 저장했다.
- Codex가 빌드·실행하고 사용자가 클릭 전후 화면을 확인했다.

### 성공한 것

- 최초 앱과 수정 후 앱의 빌드 성공. 마지막 빌드는 경고 0개, 오류 0개였다.
- 사용자 제공 화면으로 글자 크기 변경과 새 문장 표시를 확인했다.
- 화면 설정(XAML)과 클릭 동작(C#)을 각각 수정하는 첫 실습을 완료했다.

### 막힌 것

- SDK가 없어 설치가 필요했다. 최초 .NET 설정에는 사용자 폴더 접근 승인이 필요했다.
- 사이드바 직접 편집 안내가 부정확해 Cursor에서 수정하는 흐름으로 정정했다.
- 실행 중인 앱이 남아 파일 교체가 막혔다. 해당 프로세스를 종료한 뒤 재빌드했다.
- Cursor와 실행 앱 중 어느 창을 닫아야 하는지 혼동이 있어 ‘Cursor는 유지, 게임 영어 도우미는 종료’로 설명했다.

### 새로 알게 된 것

C#/WPF/XAML의 역할, 속성과 함수의 차이, xmlns의 기본 의미, 문자열 대입, 저장과 빌드의 차이를 설명하고 일부를 직접 실습했다. 독립적으로 전체 코드를 작성할 수 있게 되었다는 의미는 아니다.

자세한 복습은 [오늘의 TIL](docs/til/2026-09-13.md)에 기록했다.

### 다음 작업

마이크 입력 시작·종료 방법을 먼저 정하고, 오디오 라이브러리를 검토하여 마이크 소리 수신부터 구현한다.

### 현재 범위와 검증 한계

고정 문장 표시만 구현했다. 외부 API 호출, 마이크 녹음, STT, 번역, 자동 발음 생성, 게임 오버레이는 아직 없다. AI API 사용 비용이나 지연시간을 측정할 단계도 아니다. 자동 UI 테스트는 작성하지 않았으며 빌드와 사용자 화면 확인으로 검증했다. Git 커밋·원격 게시·배포는 수행하지 않았다.

### 관련 파일

- `src/GameLanguageAssistant/GameLanguageAssistant.csproj`: .NET/WPF 빌드 설정.
- `src/GameLanguageAssistant/App.xaml`, `App.xaml.cs`: 앱 시작 설정과 클래스.
- `src/GameLanguageAssistant/MainWindow.xaml`: 화면 구성.
- `src/GameLanguageAssistant/MainWindow.xaml.cs`: 클릭 시 고정 문장 표시.
- `README.md`: 실행과 파일 역할 안내.
- `.gitignore`: 빌드 결과물 등 제외 설정.

추천 커밋 메시지: `feat: add first WPF example window`
