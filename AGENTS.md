# 프로젝트 작업 지침

이 지침은 프로젝트 전체에 적용한다. 제품 요구사항과 개발 단계의 기준은 `PROJECT_SPEC.md`이며, 이 파일은 개발 협업 방식을 보완한다. 사용자의 최신 명시적 요청을 우선한다.

## 개발 방향

- 실제 제품 개발과 배포를 목표로 진행한다. 학습만을 위한 예시 코드 변경이나 별도 실습을 임의로 추가하지 않는다.
- 사용자는 필요한 학습을 직접 찾아보거나 별도로 질문하며 진행한다. 매 작업을 강의나 실습 과제로 만들지 않는다.
- 사용자가 읽기 쉽도록 코드를 입문자 수준에 억지로 맞추지 않는다. 실용성, 정확성, 유지보수성을 기준으로 작성한다.
- 현재 단계에 필요한 구조를 사용하고, 불필요한 기술이나 과도한 추상화는 추가하지 않는다.
- `PROJECT_SPEC.md`의 단계별 범위와 작은 작업 단위 원칙을 유지한다. 제품 동작이 불분명하면 임의로 구현하지 말고 확인한다.

## 설명 방식

- 구현 전에는 무엇을 만들고 왜 필요한지, 주요 대안과 선택 이유를 간결하게 설명한다.
- 구현 후에는 변경한 파일과 역할, 핵심 실행 흐름, 주요 함수, 외부 API 데이터 흐름이 있다면 그 내용, 검증 결과와 한계를 설명한다.
- 설명은 사용자가 소스코드의 흐름을 읽고 이해하는 데 도움이 되는 수준으로 제공한다. 학습을 위한 수동 수정 과제를 기본 단계로 넣지 않는다.
- 낯선 개발 용어는 영어와 한글 발음, 뜻을 함께 안내한다. 예: `Amend(어멘드, 가장 최근 커밋을 고쳐 다시 만들기)`.
- Name, URL, Commit 등 사용자가 이미 안다고 밝힌 기본 단어는 발음과 뜻을 반복해서 설명하지 않는다.
- 설명을 들었다는 사실만으로 사용자가 개념을 숙달했다고 가정하지 않는다.

## 개발 기록

- TIL과 개발 일지는 실제 작업, 선택 이유, 문제 해결, 검증 결과를 바탕으로 작성한다.
- 사용자가 직접 수행한 작업과 Codex가 수행한 작업을 구분한다. 설명한 내용과 사용자가 직접 확인한 내용을 구분하고 학습 성과를 과장하지 않는다.
- `DEVLOG.md`에는 개발 진행 상황을, `docs/TIL/`에는 복습할 개념을, `docs/DECISIONS.md`에는 중요한 기술 결정을, `docs/TROUBLESHOOTING.md`에는 실제 문제 해결 과정을 기록한다.
- 당일 TIL 정리는 사용자가 요청한 시점에 수행한다. 코드 변경만으로 학습용 문서를 불필요하게 늘리지 않는다.
- 사용자가 개발 일지와 TIL 정리 또는 당일 작업 마무리를 요청하면 문서 작성·검증뿐 아니라 별도 문서 커밋과 GitHub Push까지 완료한다. 매번 게시 여부를 다시 묻지 않는다.
- 한국 시간 자정 전 게시를 우선한다. 코드가 미완성이거나 함께 커밋하기 어려워도 개발 일지와 TIL은 실제 상태·미해결 사항을 적어 코드와 분리해 커밋·Push한다. 미완성 코드를 문서 게시 때문에 임의로 포함하지 않는다.
- Push 실패나 인증·권한 문제가 발생하면 해결을 시도하고, 게시 여부와 남은 장애를 명확히 알린다. 자정 이후라면 실제 날짜·시각으로 기록하며 커밋 날짜를 소급하지 않는다.

기록일: 2026-09-14. 사용자가 요청한 개발 및 설명 방식에 따라 작성했다.

추가 지침: 2026-09-15. 개발 일지·TIL 정리 요청에는 별도 문서 커밋·Push까지 포함한다.
